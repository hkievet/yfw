{
  "name": "yfw-pipeline",
  "version": "1.0.0",
  "description": "1. Need a cmd line interface to call with a youtube url that will download the url to videos.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm run server:watch --workspace @yfw/server",
    "client": "npm run dev --workspace @yfw/client",
    "build": "npm run build --workspace @yfw/core"
  },
  "bin": {
    "yfw": "./packages/yfw/src/yfw.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^9.4.1",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.1"
  },
  "engines": {
    "npm": ">=8.0.0 <9.0.0",
    "node": ">=16.0.0 <17.0.0"
  }
}